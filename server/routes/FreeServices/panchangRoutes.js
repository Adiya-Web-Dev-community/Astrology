const express = require("express");
const router = express.Router();
const panchangController = require("../../controllers/FreeServices/panchangeController");

// Route to get Panchang data
// router.get("/panchang", panchangController.getPanchangData);

module.exports = router;
